[tool.coverage.run]
source = ["app"]
omit = [
    "app/db/seed.py",
    "tests/*",
]

[tool.coverage.report]
show_missing = true
skip_empty = true
fail_under = 60
exclude_lines = [
    "pragma: no cover",
    "if __name__ == .__main__.",
]
